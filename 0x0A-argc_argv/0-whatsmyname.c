#include <stdio.h>

/**
 * main - Entry point of program
 * @argc: - The count of command-line argument
 * @argv: - An array of strings containing arguments
 *
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
